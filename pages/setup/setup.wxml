<view class="local">
  <view class="central">
    <view class="top">
      <view>
        <image src="../../img/mipmap-mdpi/touxiang.png" class="img"></image>
      </view>
    </view>
    <view>
      <van-cell-group>
        <van-field value="{{ username3 }}" label="用户名" placeholder="请输入用户名" input-align="right" />
        <van-field value="{{ phone }}" label="手机号" placeholder="请输入手机号" input-align="right" clearable="true" />
        <van-field value="{{ phone }}" label="生日" input-align="right" right-icon="arrow" />
        <van-field value="{{ phone }}" label="修改密码" input-align="right" right-icon="arrow" />
        <van-field value="{{ phone }}" label="关于我们" input-align="right" right-icon="arrow" />
        <van-field value="{{ phone }}" label="常见问题" input-align="right" right-icon="arrow" />
        <van-field value="{{ phone }}" label="用户协议" input-align="right" right-icon="arrow" />
        <van-field value="{{ phone }}" label="清除缓存" input-align="right" right-icon="arrow" />
      </van-cell-group>
    </view>
    <view class="immediately">
      <button class="login-bu" bindtap="Logout">退出登录</button>
    </view>
  </view>
</view>